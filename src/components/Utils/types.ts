export interface ProfileType {
  name: string;
  email: string;
  location: string;
  occupation: string;
}

export interface AlbumType {
  albumId: number;
  id: number;
  title: string;
  url: string;
  thumbnailUrl: string;
}
